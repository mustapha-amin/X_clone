import 'package:flutter/material.dart';

class AppColors {
  static const blueColor = Color.fromARGB(255, 29, 155, 240);
  static const darkModeColor = Colors.black;
  static const lightModeColor = Colors.white;
}
