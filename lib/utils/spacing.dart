import 'package:flutter/material.dart';

Widget VerticalSpacing({double? size}) => SizedBox(
      height: size,
    );
Widget HorizontalSpacing({double? size}) => SizedBox(
      width: size,
    );
